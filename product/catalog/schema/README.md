# Production catalog schema

This directory owns the production PDCM catalog schema. Test-only metric
descriptors and values must live under `testkit/catalogs` and must never be
installed from this directory.
